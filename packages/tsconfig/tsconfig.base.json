{
  "$schema": "https://json.schemastore.org/tsconfig.json",
  "compilerOptions": {
    // Type Checking
    "noUnusedLocals": false,
    "noUnusedParameters": false,
    "strict": true,
    "strictNullChecks": true,
    // Modules
    "module": "ES2022",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    // "rootDir": "src",
    // "types": [],
    // Emit
    "declaration": true,
    "declarationMap": true,
    "inlineSources": false,
    // Note: outDir is intentionally omitted. A relative outDir in a shared
    // config resolves against THIS file's location, not the consumer's, so
    // each package must set its own outDir in its tsconfig.json.
    // JavaScript Support
    "allowJs": true,
    // Editor Support
    // Interop Constraints
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "isolatedModules": true,
    // Backwards Compatibility
    // Language and Environment
    // "jsx": "react-jsx",
    "lib": ["ES2022"],
    "target": "ES2022",
    // Compiler Diagnostics
    // Projects
    "composite": false,
    "incremental": false,
    // Output Formatting
    "preserveWatchOutput": true,
    // Completeness
    "skipLibCheck": true
  },
  "exclude": ["node_modules"]
}
